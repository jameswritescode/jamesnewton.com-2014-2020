$(function() {
  $('.js-typed').typed({
    strings:   ['a cat owner', 'a wantrepreneur', 'a tamagotchi', 'a musician',
                'a schmuck', 'a drummer', 'an emoji enthusiast',
                'a software developer'],
    backDelay: 1000
  });
});
